import type { Plugin } from 'esbuild';
import { DefaultOptions as MinifyOptions } from 'minify-html-literals';
export interface Options extends MinifyOptions {
    /** filter to apply to file paths */
    filter?: RegExp;
}
export declare function minifyHTMLLiteralsPlugin(options?: Options): Plugin;
